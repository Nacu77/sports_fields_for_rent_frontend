import { ShowControlDirective } from './show-control.directive';

describe('ShowControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowControlDirective();
    expect(directive).toBeTruthy();
  });
});
